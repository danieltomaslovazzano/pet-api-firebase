const axios = require('./helpers/request'); describe('E2E: Unified Response Structure', () => { it('should validate organization types endpoint', async () => { const response = await axios.get('http://localhost:3000/api/organizations/types'); expect(response.data).toHaveProperty('success'); expect(response.data).toHaveProperty('data'); expect(response.data).toHaveProperty('meta'); }); });
